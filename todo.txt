minimum # of chars in search

admin page with password

test stripping, i.e. markdown links with embedded images

sort by owner name, repo name, # matches, starred date

limit length of each single result line

add links to the bottom of the search page

spinner after starting search

fixed search form layout
